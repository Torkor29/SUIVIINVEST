{
  "name": "@suiviinvest/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API HTTP : patrimoine, connecteurs read-only, imports, market data, sauvegardes, scheduler.",
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^8.0.3",
    "@node-rs/argon2": "^2.0.2",
    "@suiviinvest/api-contract": "*",
    "@suiviinvest/connectors": "*",
    "@suiviinvest/core": "*",
    "croner": "^8.1.2",
    "fastify": "^5.2.0",
    "zod": "^3.24.1"
  }
}